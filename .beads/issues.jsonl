{"id":"version-manager-70i","title":"Evaluate PR 4: package-json version mode (commit-time version bump)","description":"PR 4 (branch claude/add-alternate-version-mode-Hnq9X) adds versionMode: \"package-json\" — a pre-commit hook that computes the next version and writes it into package.json, so the committed package.json always carries the real version. Goal (Justin): get the dynamic-version effect WITHOUT dynamic-version.local.json, which breaks CI (file is gitignored, remote envs must regenerate it) and breaks Expo EAS builds (which historically packed the repo without .git, making generation impossible). Target repos: eslint-config-jha-react-node, prompts — small, linear-history, low-ceremony.\n\nWritten ~2026-03 by Claude 4 Opus. Rebased onto main 2026-09-16 as local branch pr-4-package-json-mode (3 commits, one conflict in src/git-hooks-manager.ts resolved by keeping mains nonFatalSuffix and applying it to post-* hooks in both modes, deliberately NOT to pre-commit).","design":"VERDICT: the core idea is sound and worth keeping, but only for linear-history repos, and the implementation needs fixes before it is safe.\n\n=== WHAT WORKS (verified by tests/integration/package-json-mode.test.ts, 16 tests) ===\n- Sequential commits increment correctly in both calculation modes.\n- Self-healing after --no-verify works exactly as designed: version set i commits ago yields +(i+1).\n- The bumped version lands INSIDE the commit; working tree is left clean.\n- package.json alone is sufficient for a consumer — no .git, no generated file. This is the goal of the PR and it is met.\n\n=== VERIFIED BY TEST OR PROBE ===\n1. npm STRIPS build metadata on publish. Probe: `npm publish --dry-run` on version 1.0.0+5 produced \"publishprobe@1.0.0\" and filename publishprobe-1.0.0.tgz. So append-commits — the DEFAULT calculation mode — makes every commit publish as the same version; the registry rejects the second. add-to-patch must be the default for this mode. (Closed as not-a-blocker in 70i.1: Justin does not publish to npm.)\n2. Unequal-length branches ALWAYS conflict on package.json (test). Equal-length branches are subtler and worse: both sides compute the SAME version, git auto-merges cleanly, and the merged result undercounts (two commits of work, one version) (test). 70i.10 (branch suffix) makes that case a visible conflict.\n3. The design-doc claim \"on the merge commit the pre-commit hook recalculates\" is FALSE (test). git merge fires pre-merge-commit, not pre-commit; auto-merge commits keep a stale version.\n4. git commit --amend re-runs pre-commit and double-bumps (test).\n5. Rebase never re-runs the hook: the branch gains a commit while the version stands still (test).\n6. The hook reads/writes the WORKING TREE package.json then `git add`s the whole file, so an unstaged half-finished package.json edit is silently swept into the commit (test). Data integrity, not just a `git add -p` annoyance. Fixed by 70i.3.\n7. updateLockfile() buys nothing. Probes: bun.lock records the root name but NOT the root version (grep: zero \"version\" keys), and `bun install --frozen-lockfile` after a version-only bump exits 0; package-lock.json DOES record the root version but `npm ci` after a version-only bump also exits 0. `npm version X --no-git-tag-version --offline` updates both files instantly and offline if the drift is ever worth closing. Removed by 70i.5.\n8. A failing package manager inside the hook is swallowed: a stub npm that exits 1 (proven invoked via a marker file) leaves the commit succeeding with no signal (test). \"Failure is not empty\" violation. Fixed by 70i.4.\n\n=== INFERRED FROM CODE READING, NOT DEMONSTRATED ===\n9. stageFiles() also catches everything and only console.warn()s. If `git add` itself failed, package.json would be rewritten on disk but never staged, so the commit would record the OLD version while the tree shows the new one. I did not induce a git-add failure; this is a read of src/script-manager.ts, not an observation.\n10. installCommand() calls generateVersionFile() (which creates version-manager.json with the default dynamic-file) BEFORE getVersionMode(), and no --mode flag exists — so a first install appears unable to produce package-json hooks; the user must install, hand-edit config, install again. Read from src/index.ts ordering; not executed end-to-end. (70i.7, not in the current push.)\n11. installGitHooks() only writes the current mode's hooks and has no removal path, so switching package-json -> dynamic-file appears to leave pre-commit live forever. The leftover-file half IS covered by a test; that the leftover hook keeps bumping on later commits is inferred. (70i.6, not in the current push.)\n12. RESERVED_VERSION_NAMES gains \"main\" with no rationale anywhere in the diff or the plan. Observation, not a defect.\n\n=== STYLE / RULES ===\n- docs/plans/2026-03-27_committed-version-modes.md violated the current critical rules (beads replaced docs/plans/). DONE 2026-09-19 (70i.9): deleted in 35987e0; content preserved above.\n- calculatePreCommitVersion() duplicates its add-to-patch arithmetic three times.\n- preCommitHandler(outputPath: string) is typed string but yargs supplies undefined (--output has no default); the ?? fallback is load-bearing while the type says it cannot fire.\n\n=== CONDUCTOR DECISIONS, 2026-09-19 (binding on every dispatch in this push) ===\n\nD1. SCOPE OF THIS PUSH. 70i.10, then 70i.2, 70i.3, 70i.5+70i.4, then 70i.8 if budget allows. 70i.6 and 70i.7 are explicitly NOT in this push. 70i.9 is done.\n\nD2. BRANCH SUFFIX FORMAT (70i.10). `<core>-<sanitised-branch>.<n>` with the prerelease inserted before any +build metadata. Examples: 0.32.3 -> 0.32.3-feat-x.3; 0.32.1+3 -> 0.32.1-feat-x.3+3. A hyphen prerelease sorts BEFORE the release, which is correct: a branch build is a prerelease of the version it will become.\n\nD3. VERSION-MANAGER OWNS THE PRERELEASE SEGMENT WHEN THE KNOB IS ON. This is the load-bearing decision. In package-json mode the decorated version is COMMITTED into package.json, so the next computation reads it back as its own input — a feedback loop that would otherwise compound (0.1.0-b.1 -> 0.1.0-b.1-b.2). Therefore: every computation strips the entire existing prerelease from the current/base version before calculating, and re-applies a freshly computed suffix at the end. Consequence, accepted and documented: with the knob ON, a hand-authored prerelease in package.json (1.0.0-beta.1) is discarded on the next hooked commit. The knob is off by default; this limitation is documented in the README and pinned by a test. REJECTED ALTERNATIVE: a marker token (0.32.1-branch.feat-x.3) that makes the suffix recognisable so hand-authored prereleases survive — rejected because it deviates from the shape Justin wrote down and buys a case he does not have. Revisit only if a real base prerelease appears.\n\nD4. SANITISATION, exact rules. (a) Replace every character outside [0-9A-Za-z-] with '-' (so slashes AND dots in branch names both become '-'; the only dot in the suffix is the one before n). (b) Collapse runs of '-' to one. (c) Trim leading/trailing '-'. (d) Empty result -> the literal 'branch'. (e) All-digits result -> prefix 'b' (semver forbids leading zeros in a numeric identifier; 007 -> b007). Sanitisation is lossy and may collide (feat/x and feat-x both -> feat-x); that is accepted and documented.\n\nD5. WHAT n IS. n = commits on this branch since its merge base with the main branch, measured by `git rev-list --count <mainBranch>..HEAD`, plus 1 in the pre-commit path for the about-to-happen commit. Reason: the count the modes already compute is useless here — in package-json mode the version changes on EVERY commit, so commitsSinceLastChange is always 0 and n would always be 1. If no configured main branch ref resolves, fall back to `git rev-list --count HEAD` (total commits) and say so in verbose output. If BOTH measurements fail, apply NO suffix and return the undecorated version — never substitute 0 for a failed measurement (critical rule 6).\n\nD6. WHICH BRANCHES ARE \"MAIN\". config.branchSuffix.mainBranches, default [\"main\", \"master\"]. On a listed branch, and in detached HEAD (getCurrentBranch returns \"HEAD\"), no suffix is applied.\n\nD7. n === 0 MEANS NO SUFFIX. A branch with no commits of its own is bit-identical to its base, so it gets the undecorated version. Prevents 0.32.1-my-branch.0 sorting below the release it equals.\n\nD8. THE KNOB APPLIES TO BOTH versionModes. dynamic-file gets it in the generated file (no feedback hazard, base always comes from an undecorated package.json); package-json gets it committed. Same config field, same helper, one code path.\n\nD9. INDEX IS THE SOURCE OF TRUTH IN PRE-COMMIT (70i.3). The current version is read from the INDEX (`git show :package.json`), which equals HEAD's content when package.json is unstaged — one rule covers both cases. The new content is written to the index surgically (hash-object + update-index), and the working-tree file gets only its version field rewritten, so unstaged edits elsewhere in package.json survive and are NOT staged.\n\nD10. FAIL LOUDLY IN THE HOOK (70i.4). A failed stage or a failed package-manager call aborts the commit with a non-zero exit and a message naming the exact failed command. A commit that records the wrong version is worse than a blocked commit, and `--no-verify` self-heals by design.\n\nD11. MERGE DRIVER POLICY (70i.8). Justin, 2026-09-19: default to OURS (the branch being merged into, i.e. main) for the version field. The pre-commit hook then adds exactly 1 on top, so a squash of a 500-commit branch lands at main+1.\n\nD12. NO NEW dynamic-version.local.json IN package-json MODE (70i.2). In package-json mode nothing is written unless --output is passed explicitly; the post-* hooks are not installed, and the *.local.json gitignore prompt is skipped. The watcher and the metro plugin still write the file in package-json mode — out of scope here, filed as 70i.11.","acceptance_criteria":"Justin has: (a) a clear verdict on whether the approach is worth keeping, (b) verified evidence for each claim rather than inference, (c) a prioritised list of what must change before it is safe to use, (d) automated tests that pin the behaviour so a future fix flips a documented expectation.","notes":"Test baseline MEASURED 2026-09-19 on a clean clone of main (fd700e5) and on pr-4-package-json-mode, full suite both sides: main 86 pass / 1 skip / 8 fail; branch 122 pass / 1 skip / 8 fail. IDENTICAL failing set, so the branch adds zero regressions and 36 passing tests:\n  formatVersionOutput > compact mode > renders single line with version, branch, and checkmark\n  formatVersionOutput > compact mode > shows dirty indicator when dirty\n  Watch Command > starts watcher and watches for file changes\n  Watch Command > debounces rapid file changes\n  Watch Command > respects .gitignore patterns\n  Watch Command > watches git state changes (commits)\n  Watch Command > watches version-manager.json changes\n  Git Hooks Installation > Husky Installation > detects bun package manager from bun.lock\nThe earlier '7 pre-existing failures' figure was one short: the husky bun.lock failure is pre-existing on main too (verified by running that file against the main clone).\n\nFLAKE, not a defect: under full-suite load, package-json-mode integration tests hit bun's default 5s per-test timeout and report as failures; the same file run alone is 16/16 in 29s. Every new integration test in this push must set an explicit per-test timeout of at least 20000ms.\n\nTEST RECIPE: run with npm_config_cache pointed into $TMPDIR, or the install tests EPERM on ~/.npm/_cacache in the sandbox. That sandbox artifact is what produced the '23 failures' seen on a first run in an earlier session.\n\nFixture harness notes: hooks must be committed (amended into the config commit) or they vanish on branch switch and every count shifts; node_modules/ must be gitignored in the fixture because the hooks' npm install materialises it even in a dependency-free repo.\n\nLint: FIXED 2026-09-19 in 35987e0. eslint.config.js (the config ESLint 9 actually loads, NOT eslint.config.cjs) now ignores **/.claude/worktrees/. bun run signal is fully green on the branch.","status":"open","priority":2,"issue_type":"epic","created_at":"2026-09-16T14:33:58.293805Z","created_by":"jhaa","updated_at":"2026-09-19T11:09:44.188605Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"version-manager-70i.1","title":"Default to add-to-patch in package-json mode (npm strips +N on publish)","notes":"Closed 2026-09-16: Justin said \"I do not currently publish almost anything to npm, so npm publish does not have to be a blocker.\" The finding stands (npm strips +N on publish, verified by npm publish --dry-run) but it is not a reason to change the default for him. If the branch-suffix idea (70i.10) lands, prerelease identifiers survive publish anyway, which makes this moot from a second direction.","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-16T14:46:51.517025Z","created_by":"jhaa","updated_at":"2026-09-16T15:01:16.903340Z","closed_at":"2026-09-16T15:01:16.902789Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"version-manager-70i.1","depends_on_id":"version-manager-70i","type":"parent-child","created_at":"2026-09-16T14:46:51.517025Z","created_by":"jhaa","metadata":"{}","thread_id":""}]}
{"id":"version-manager-70i.10","title":"Branch-name suffix in version when not on main (0.32.1-my-branch.3)","description":"Justin's idea (2026-09-16): when not on the main branch, include the branch name in the computed version, e.g. 0.32.1-my-branch-name. He said it \"seems potentially useful\".\n\nWHAT IT FIXES: the silent-collision case measured in 70i.8. Today two branches that add the SAME number of commits compute the IDENTICAL version, so git auto-merges the version field with no conflict and the merged result undercounts. A branch suffix makes the two sides structurally different, so that case becomes a visible conflict instead of a quiet wrong answer. Strict improvement in failure mode.\n\nWHAT IT DOES NOT FIX: it does not reduce merge conflicts — it increases them. Both sides still rewrite the same line. Only a merge driver (70i.8) removes the conflict.\n\nINDEPENDENT VALUE: a build labelled 0.32.1-my-branch.3 is identifiable on a device at a glance, which is close to the original reason version-manager exists.\n\nSEMVER DETAIL: a hyphen suffix is a PRERELEASE, not build metadata. Precedence puts 0.32.1-foo BEFORE 0.32.1, which is semantically right — a branch build is a prerelease of the version it will become. Prerelease identifiers are also NOT stripped by npm publish, unlike the +N build metadata in 70i.1.\n\nIN SCOPE (function level)\n- New module src/branch-suffix.ts (no barrel files; direct imports). Pure functions, no I/O:\n  - sanitizeBranchName(branch: string): string — rules in D4 of the epic design.\n  - stripPrerelease(version: string): string — removes the prerelease segment, preserves +build metadata.\n  - applyBranchSuffix(version: string, sanitisedBranch: string, n: number): string — inserts \"-<branch>.<n>\" after the version core and BEFORE any +build metadata.\n- Config: add branchSuffix to VersionManagerConfigSchema in src/types.ts. The schema is .strict(), so the field must be declared or configs carrying it are rejected. Exact shape:\n    branchSuffix: z.object({\n      enabled: z.boolean().default(false),\n      mainBranches: z.array(z.string()).default(['main', 'master']),\n    }).default({enabled: false, mainBranches: ['main', 'master']})\n  Default must survive a config file that omits the field entirely AND a config that supplies a partial object.\n- Git: a counting helper in src/git-utils.ts that returns commits since the merge base with a main branch. It must return number | null — null when the ref does not resolve. Never 0 on failure (critical rule 6).\n- Wire into BOTH computation paths: generateFileBasedVersion() and generatePreCommitVersionData() in src/version-generator.ts.\n- README: document the knob, the format, the sanitisation rules, and the D3 limitation.\n\nTHE FEEDBACK LOOP — the thing most likely to be got wrong\nIn package-json mode the decorated version is COMMITTED into package.json and read back as the input to the next computation. calculatePreCommitVersion() and calculateCodeVersion() both do version.split('.') and return the input UNCHANGED when the result is not exactly 3 parts. A prerelease makes 4 parts, so without stripping, the version silently freezes forever. Therefore: strip the prerelease BEFORE either calculate function sees the string, and re-apply a freshly computed suffix AFTER. Per epic D3, version-manager OWNS the prerelease segment while the knob is on; a hand-authored prerelease is discarded, documented, and pinned by a test.\n\nSTRUCTURE REQUEST (makes 70i.3 cheap)\nIn the pre-commit path, isolate \"read the current version\" and \"write the new version\" into two small named helpers. 70i.3 will replace how those two read and write (index instead of working tree) and should be able to swap the helper bodies without untangling suffix logic from file I/O.\n\nOUT OF SCOPE\n- The merge driver (70i.8).\n- Changing the default versionCalculationMode (70i.1, closed).\n- The --mode install flag (70i.7) and stale-hook removal (70i.6).\n- Removing dynamic-version.local.json writes (70i.2).\n- Making the watcher or the metro plugin branch-suffix-aware beyond what falls out of using the shared code path.","design":"DESIGN DECISIONS ARE IN THE EPIC (version-manager-70i) design field, items D2 through D8. Read them before starting; they are binding. Summary:\n- Format <core>-<sanitised-branch>.<n>, prerelease inserted before any +build metadata (D2).\n- version-manager OWNS the prerelease segment while the knob is on; strip before calculating, re-apply after (D3). This is the decision that prevents the feedback loop.\n- Exact sanitisation rules (D4).\n- n = commits since the merge base with the main branch, +1 in pre-commit; fallbacks and the no-measurement case (D5).\n- mainBranches config, detached HEAD excluded (D6).\n- n === 0 means no suffix (D7).\n- Applies to both versionModes through one code path (D8).","acceptance_criteria":"GATE. `bun run signal` clean, and `bun test` showing no failure that main does not also have (measured baseline is recorded in the epic notes). Run the suite with npm_config_cache redirected into $TMPDIR or the install tests EPERM. New tests in this file get an explicit per-test timeout (>= 20000ms); the default 5s timeout makes package-json-mode tests flake under full-suite load.\n\nBEHAVIOUR (each item needs a test)\n1. Off by default. A config with no branchSuffix field, and a repo with no version-manager.json at all, both produce today's undecorated versions in both versionModes.\n2. Sanitisation: branch \"claude/add-alternate-version-mode-Hnq9X\" produces a valid semver; so do branch names containing dots, spaces, and consecutive separators. Assert validity with semver.valid() from the `semver` package added as a DEV dependency only (runtime dep count stays where CLAUDE.md wants it), not with a hand-rolled regex.\n3. Sanitisation edge cases: an all-punctuation branch name yields the literal \"branch\"; an all-digits branch name is prefixed with \"b\" (leading zeros are illegal in a semver numeric identifier).\n4. main is untouched: on \"main\" and on \"master\" the version is undecorated with the knob ON. Detached HEAD (getCurrentBranch() returns \"HEAD\") is also undecorated.\n5. n === 0 yields no suffix.\n6. Equal-length branches no longer collide: the existing package-json-mode test that shows two equal-length branches auto-merging to the same undercounted version stays as the knob-OFF control, and a knob-ON twin shows the two sides producing DIFFERENT versions so the merge conflicts visibly instead of silently undercounting.\n7. No feedback loop in package-json mode: with the knob on, successive commits on a branch go 0.1.1-feat-x.1 -> 0.1.2-feat-x.2 (add-to-patch) rather than compounding suffixes or freezing. Cover append-commits too: the prerelease sits before the +N metadata.\n8. Both versionModes: dynamic-file writes the decorated version into dynamic-version.local.json; package-json commits it into package.json.\n9. Failure is not empty: when the configured main branches do not resolve, the count falls back to the total commit count and the version is still produced; when no count can be measured at all, the UNDECORATED version is returned. No path may substitute 0 for a failed measurement.\n\nNEGATIVE CONTROLS (required, and reported)\nFor at least the sanitisation test, the main-untouched test, and the feedback-loop test: break the implementation, watch the test fail, restore it, and report what the failure message was. An unproven test is no test.","status":"in_progress","priority":2,"issue_type":"feature","created_at":"2026-09-16T15:01:10.328371Z","created_by":"jhaa","updated_at":"2026-09-19T11:08:59.352074Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"version-manager-70i.10","depends_on_id":"version-manager-70i","type":"parent-child","created_at":"2026-09-16T15:01:10.328371Z","created_by":"jhaa","metadata":"{}","thread_id":""}]}
{"id":"version-manager-70i.11","title":"Watcher and metro plugin still write dynamic-version.local.json in package-json mode","description":"70i.2 stops the CLI and the git hooks from writing dynamic-version.local.json in package-json mode. src/watcher.ts and src/metro-plugin.ts were deliberately left out of that change and still write the file on every trigger, which reopens exactly the hole package-json mode exists to close.\n\nFIX SHAPE: both call into the same generate path; make them respect the same mode decision 70i.2 centralises. A watcher running in package-json mode has nothing useful to write, so it should either no-op with one clear message at startup or refuse to start.","acceptance_criteria":"A watcher run and a metro bundle in package-json mode both leave no dynamic-version.local.json, proven by a test. dynamic-file behaviour unchanged.","status":"open","priority":2,"issue_type":"bug","created_at":"2026-09-19T11:09:39.032485Z","created_by":"jhaa","updated_at":"2026-09-19T11:09:44.102173Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"version-manager-70i.11","depends_on_id":"version-manager-70i","type":"parent-child","created_at":"2026-09-19T11:09:39.032485Z","created_by":"jhaa","metadata":"{}","thread_id":""}]}
{"id":"version-manager-70i.2","title":"Stop writing dynamic-version.local.json in package-json mode","description":"THE POINT OF THE WHOLE PR. Justin's stated goal for package-json mode: get the dynamic-version effect WITHOUT dynamic-version.local.json. The generated file breaks CI (it is gitignored, so remote environments must regenerate it, which needs .git) and breaks Expo EAS builds (which historically packed the repo without .git, making generation impossible). PR 4 added the commit-time bump but left every generated-file code path in place, so package-json mode currently does BOTH. This bead removes the second half.\n\nIN SCOPE\n- The default (generate) command in package-json mode writes NO file. It still computes and prints the version. Exception: if --output was passed EXPLICITLY on the command line, honour it — that is unambiguous user intent. An --output that came from the yargs default does not count; check argv for the explicit flag rather than comparing against the default string.\n- Do not write dynamic-version.local.d.ts in package-json mode either, under the same rule.\n- install in package-json mode installs ONLY the pre-commit hook. The post-commit, post-checkout, post-merge and post-rewrite hooks exist solely to regenerate the file; with no file to regenerate they are dead weight and cause spurious work on every checkout. Removing hooks that a PREVIOUS install left behind is 70i.6 and is NOT in this bead — install must simply stop adding them.\n- install in package-json mode does not add the prebuild/predev/prestart scripts whose only job is regenerating the file, and skips the \"*.local.json is not in .gitignore\" prompt. Check what install actually adds before deciding; the rule is \"anything whose only purpose is the generated file\".\n- The metro plugin and the watcher STILL write the file in package-json mode. That is a real hole, deliberately left: it is filed as 70i.11 and is out of scope here.\n\nOUT OF SCOPE\n- dynamic-file mode behaviour must not change at all. Every existing dynamic-file test must still pass untouched.\n- Removing hooks or scripts installed by an earlier run (70i.6).\n- The --mode flag (70i.7).\n\nWHY IT IS NOT JUST DELETING A WRITE CALL. generateVersionFile() in src/index.ts is the shared entry point and it both computes and writes. Separate the compute from the write rather than branching inside the writer, so the CLI output path and the hook path make the same decision in one place.","acceptance_criteria":"GATE. `bun run signal` clean. `bun test` with no failure main does not also have (baseline in the epic notes); run with npm_config_cache redirected into $TMPDIR or the install tests EPERM. New integration tests get an explicit per-test timeout of at least 20000ms.\n\nBEHAVIOUR (each item needs a test)\n1. In package-json mode, a hooked commit leaves NO dynamic-version.local.json and NO dynamic-version.local.d.ts anywhere in the repo, while package.json carries the bumped version.\n2. In package-json mode, running the generate command with no flags writes no file and still prints the computed version to stdout.\n3. In package-json mode, running the generate command with an explicit --output PATH writes that file.\n4. In package-json mode, install writes a pre-commit hook and does NOT write post-commit, post-checkout, post-merge or post-rewrite version-manager lines.\n5. In package-json mode, install does not add the generated-file lifecycle scripts and does not prompt about *.local.json in .gitignore.\n6. Regression: in dynamic-file mode all four post-* hooks, the lifecycle scripts, the gitignore prompt and the generated file behave exactly as before. The existing dynamic-file tests must pass unmodified — if a test needs changing, that is a signal the change leaked.\n\nNEGATIVE CONTROLS (required, and reported)\nFor items 1 and 6: break the implementation, watch the test fail, restore it, report the failure message.","status":"open","priority":1,"issue_type":"feature","created_at":"2026-09-16T14:46:51.607285Z","created_by":"jhaa","updated_at":"2026-09-19T11:09:30.128781Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"version-manager-70i.2","depends_on_id":"version-manager-70i","type":"parent-child","created_at":"2026-09-16T14:46:51.607285Z","created_by":"jhaa","metadata":"{}","thread_id":""}]}
{"id":"version-manager-70i.3","title":"Pre-commit hook must read the INDEX, not the working tree","description":"DATA INTEGRITY BUG, demonstrated by a test already on this branch. The pre-commit hook reads the WORKING TREE package.json, rewrites its version, then runs `git add package.json` on the whole file. Any unstaged edit sitting in package.json — a half-added dependency, a script being drafted — is silently swept into a commit the author never staged it for. This is not a `git add -p` annoyance; it puts content into history that the author did not choose to commit.\n\nTHE CONTRACT (epic design D9)\n- The CURRENT version is read from the INDEX: `git show :package.json`. When package.json is unstaged this returns HEAD's content, so one rule covers both the staged and the unstaged case with no branching.\n- The NEW index content is the index content with ONLY the version value replaced. Write it with `git hash-object -w --stdin` followed by `git update-index --cacheinfo 100644,<sha>,package.json`, so nothing else about the index moves.\n- The WORKING TREE file gets only its version value rewritten, as a STRING-LEVEL replacement on the working-tree bytes. Do NOT parse and re-stringify: the whole point is to preserve a half-edited file, and a reformat destroys it. Unstaged edits elsewhere in the file survive and stay unstaged.\n- If package.json is absent from the index (a brand-new repo with nothing committed), fall back to the working tree and say so in verbose output.\n\nIN SCOPE\n- The read and write helpers in the pre-commit path (70i.10 isolates them; swap their bodies).\n- A git-utils helper to read a path's blob from the index, returning string | null — null strictly means \"not in the index\", never \"read failed\". A read failure must surface as a thrown error (critical rule 6).\n- Keep the existing behaviour that the version DOES land in the commit even when the author staged nothing in package.json. That is the mode's purpose, not a bug.\n\nOUT OF SCOPE\n- Failing loudly when staging fails (70i.4) — that is the next bead and touches the same call site. Do not pre-empt it.\n- Anything about merges, amend or rebase (70i.8).\n\nNOTE, measured: this does NOT change any merge outcome recorded in 70i.8. In both merge flows the resolved version is already STAGED, so reading the index yields exactly what reading the working tree yielded.","acceptance_criteria":"GATE. `bun run signal` clean. `bun test` with no failure main does not also have (baseline in the epic notes); npm_config_cache redirected into $TMPDIR. New integration tests carry an explicit per-test timeout of at least 20000ms.\n\nBEHAVIOUR (each item needs a test)\n1. THE HEADLINE: with an unstaged edit in package.json (add a devDependency in the working tree, stage nothing but an unrelated source file) a hooked commit records the version bump and does NOT record the devDependency. After the commit, the working tree still has the devDependency and shows the new version, and `git status` still reports package.json as modified.\n2. Staged edits to package.json ARE committed, together with the version bump, unchanged in every other respect.\n3. Formatting and key order elsewhere in the working-tree package.json survive the hook byte-for-byte apart from the version value. Use a package.json with non-default formatting (4-space indent, or a trailing comment-free but unusual key order) to prove it.\n4. The version written to the index and the version written to the working tree are the same string.\n5. The existing package-json-mode tests still pass. The self-healing-after---no-verify behaviour is unchanged.\n\nNEGATIVE CONTROLS (required, and reported)\nFor item 1 and item 3: break the implementation (revert to reading the working tree; parse-and-restringify the file), watch each test fail, restore, report the failure messages.","status":"open","priority":1,"issue_type":"bug","created_at":"2026-09-16T14:46:51.699761Z","created_by":"jhaa","updated_at":"2026-09-19T11:10:02.072565Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"version-manager-70i.3","depends_on_id":"version-manager-70i","type":"parent-child","created_at":"2026-09-16T14:46:51.699761Z","created_by":"jhaa","metadata":"{}","thread_id":""}]}
{"id":"version-manager-70i.4","title":"stageFiles/updateLockfile must fail loudly, not console.warn","description":"CRITICAL RULE 6 VIOLATION (\"failure is not empty\"), demonstrated by a test already on this branch: a stub npm that exits 1 — proven to have been invoked, via a marker file — leaves the commit SUCCEEDING with no signal at all. The same shape is in stageFiles(), which catches everything and only console.warn()s.\n\nWHY IT MATTERS HERE SPECIFICALLY. If `git add` fails and the failure is swallowed, package.json is rewritten on disk but never staged: the commit records the OLD version while the working tree shows the NEW one. Every later computation counts from the wrong anchor, and the mistake is invisible because nothing is red. This is the reassuring-direction failure the rule is about.\n\nTHE CONTRACT (epic design D10)\n- A failed stage aborts the commit: non-zero exit from the hook, and a message that NAMES the exact command that failed and its stderr. A blocked commit is strictly better than a commit that records the wrong version, and `--no-verify` already self-heals on the next hooked commit by design.\n- No catch block in this path may return a value that is also a legal success value. Where a helper can fail, its return type carries the failure as a distinct member (string | null, a result union) and the caller must handle it.\n- console.warn() is not error handling. Anything that is genuinely advisory and genuinely safe to ignore must say so in a comment stating why the commit is still correct if it fails.\n\nIN SCOPE\n- stageFiles() in src/script-manager.ts and its caller in the pre-commit path.\n- The pre-commit handler's top-level error handling: a thrown error becomes a non-zero exit with a readable message, not a stack trace and not a silent success.\n- Any other swallow-and-continue in the pre-commit path found while reading it; list each one you found and what you did about it.\n\nDEPENDENCY: do 70i.5 (drop updateLockfile) FIRST. It deletes the package-manager subprocess, which is one of the two swallowed failures, so this bead then only has to harden staging and the handler's own error path.\n\nOUT OF SCOPE\n- The post-* hooks' deliberate --no-fail behaviour in dynamic-file mode. Those exist so a version-generation hiccup never blocks a checkout, and main's nonFatalSuffix was kept for exactly that reason. Do not make them fatal.","acceptance_criteria":"GATE. `bun run signal` clean. `bun test` with no failure main does not also have (baseline in the epic notes); npm_config_cache redirected into $TMPDIR; new integration tests carry a per-test timeout of at least 20000ms.\n\nBEHAVIOUR (each item needs a test)\n1. When staging the version change fails, the commit does NOT happen: git exits non-zero, HEAD is unmoved, and stderr names the failed command. Induce the failure for real rather than by mocking the whole function — for example make .git/index unwritable for the duration, or put a failing `git` earlier on PATH for the hook's subprocess. If no honest induction can be found, say so plainly in the bead notes and in your report rather than faking one.\n2. After that failed commit the repository is not left in a half-updated state that would produce a WRONG version on the next attempt: either the working tree version change is rolled back, or the next hooked commit recomputes correctly from the index. Whichever you choose, state it in the bead and pin it with a test.\n3. The success path is unchanged: a normal hooked commit still bumps and still exits 0.\n4. Report the list of every swallow-and-continue you found in the pre-commit path and what you did about each.\n\nNEGATIVE CONTROL (required, and reported): restore the console.warn swallow, watch test 1 fail, restore the fix, report the failure message.","status":"open","priority":1,"issue_type":"bug","created_at":"2026-09-16T14:46:51.793131Z","created_by":"jhaa","updated_at":"2026-09-19T11:10:21.974895Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"version-manager-70i.4","depends_on_id":"version-manager-70i","type":"parent-child","created_at":"2026-09-16T14:46:51.793131Z","created_by":"jhaa","metadata":"{}","thread_id":""}]}
{"id":"version-manager-70i.5","title":"Drop updateLockfile from the pre-commit hook (buys nothing)","description":"MEASURED, NOT ASSUMED: updateLockfile() in the pre-commit path buys nothing, so it is pure cost — a package-manager subprocess on every single commit.\n\nThe probes behind that (2026-09-16):\n- bun.lock records the root package NAME but not the root VERSION (grep found zero \"version\" keys for the root), and `bun install --frozen-lockfile` after a version-only bump exits 0.\n- package-lock.json DOES record the root version, but `npm ci` after a version-only bump also exits 0.\n- So no supported workflow breaks from the drift it was written to close. If closing the drift is ever wanted, `npm version X --no-git-tag-version --offline` updates both files instantly and offline — no need for this code path.\n\nIN SCOPE\n- Remove updateLockfile() and its call from the pre-commit path.\n- Remove any package-manager detection, imports or helpers that exist ONLY to serve it. Check for other callers before deleting anything: detectPackageManager is also used by the husky/hook-install path, and that use must stay.\n- Remove the tests that exist only to pin updateLockfile's behaviour, and keep the one that proves a hooked commit does NOT shell out to a package manager (invert the existing stub-npm marker-file test: the marker must NOT appear).\n\nOUT OF SCOPE\n- Any change to how staging failures are reported (70i.4, the very next bead, which lands on the same call site). Do 70i.5 first so 70i.4 has less surface to harden.\n\nWHY THIS IS SAFE TO DELETE RATHER THAN DISABLE: the behaviour is recoverable from git history, no consumer can depend on a lockfile side effect of a version bump (the probes show both package managers accept the drift), and leaving it behind a flag would add a knob nobody would ever turn on.","acceptance_criteria":"GATE. `bun run signal` clean. `bun test` with no failure main does not also have (baseline in the epic notes); npm_config_cache redirected into $TMPDIR; new integration tests carry a per-test timeout of at least 20000ms.\n\nBEHAVIOUR\n1. A hooked commit in package-json mode invokes NO package manager. Proven by the inverted stub-npm test: a stub on PATH that writes a marker file is never triggered, so the marker does not exist after the commit.\n2. grep shows no remaining reference to updateLockfile anywhere in src/ or tests/.\n3. detectPackageManager (or whatever the husky/hook-install path uses) still works: the existing install tests pass unmodified.\n4. A hooked commit still bumps package.json correctly in both calculation modes.\n\nNEGATIVE CONTROL (required, and reported): re-add the call, watch the marker-file test fail, remove it again, report the failure message.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-16T14:46:51.884880Z","created_by":"jhaa","updated_at":"2026-09-19T11:10:21.871502Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"version-manager-70i.5","depends_on_id":"version-manager-70i","type":"parent-child","created_at":"2026-09-16T14:46:51.884880Z","created_by":"jhaa","metadata":"{}","thread_id":""}]}
{"id":"version-manager-70i.6","title":"Remove stale hooks when versionMode changes","status":"open","priority":2,"issue_type":"bug","created_at":"2026-09-16T14:46:51.974825Z","created_by":"jhaa","updated_at":"2026-09-16T14:46:51.974825Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"version-manager-70i.6","depends_on_id":"version-manager-70i","type":"parent-child","created_at":"2026-09-16T14:46:51.974825Z","created_by":"jhaa","metadata":"{}","thread_id":""}]}
{"id":"version-manager-70i.7","title":"Add --mode flag so a fresh install can reach package-json mode","status":"open","priority":2,"issue_type":"feature","created_at":"2026-09-16T14:46:52.070247Z","created_by":"jhaa","updated_at":"2026-09-16T14:46:52.070247Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"version-manager-70i.7","depends_on_id":"version-manager-70i","type":"parent-child","created_at":"2026-09-16T14:46:52.070247Z","created_by":"jhaa","metadata":"{}","thread_id":""}]}
{"id":"version-manager-70i.8","title":"Handle amend/rebase/merge in package-json mode (or document as linear-only)","description":"What actually happens on merges in package-json mode, measured 2026-09-16 (not inferred). Setup each time: base 0.1.0, feature +5 hooked commits -> 0.1.5, main +1 hooked commit -> 0.1.1.\n\n- TRUE MERGE (--no-ff), diverged main: CONFLICTS on package.json. Resolve ours(main 0.1.1) -> merge commit lands at 0.1.2. The 5 feature commits are not counted.\n- SQUASH MERGE, diverged main: CONFLICTS on package.json. Resolve ours -> 0.1.2. Resolve theirs(0.1.5) -> 0.1.6.\n- SQUASH MERGE, main NOT diverged: no conflict, feature version 0.1.5 carries into the index, pre-commit adds 1 -> 0.1.6.\n\nCONCLUSION: the outcome is decided entirely by which side of the version field wins the merge resolution; the pre-commit hook then adds exactly 1 on top. Both of the answers Justin floated are reachable and neither needs new algorithm work:\n  - ours(main) => squash of a 500-commit branch lands at main+1 (his \"could also just be 0.1.1\")\n  - theirs(branch) => lands at branch version +1 (his \"0.1.500 is fine by me\")\n\nSo a merge driver here is a POLICY choice, not a hard problem: it only has to pick a side for the version field and let normal JSON merging handle the rest of package.json.\n\nIMPORTANT: this does NOT conflict with 70i.3 (read the index, not the working tree). In both merge flows the resolved version is STAGED, so reading the index yields the same answer as reading the working tree. Fixing 70i.3 does not change any merge outcome measured here.","design":"RECOMMENDED: merge driver that takes OURS (the branch being merged into) for the version field only, everything else normal JSON merge. Rationale: main version then counts commits landed ON MAIN, monotonic and stable regardless of how people branched; two 500-commit branches squash-merged do not produce wild jumps. Registered via .gitattributes (package.json merge=version-manager) plus a git config driver entry, both installable by the install command.\n\nCAVEAT not yet verified: a merge driver is LOCAL git config, so it does not run for merges performed on GitHub servers (the merge/squash button). Server-side merges would still conflict and block the button. Needs checking before promising this fixes the PR workflow.","notes":"DECIDED by Justin 2026-09-19: the merge driver takes OURS (the branch being merged into, i.e. main) for the version field. The a/b is closed; build the ours policy.\n\nIMPLEMENTATION SHAPE (do not write a JSON merger). A git merge driver is invoked with %O (ancestor) %A (ours) %B (theirs) and must leave the result in %A. Read the version value out of %A, rewrite the version value in BOTH %O and %B to that same string, then run `git merge-file %A %O %B` and propagate its exit code. The version line is then identical on all three sides so it cannot conflict, and every other key in package.json merges by git's normal rules with normal conflict behaviour.\n\nREGISTRATION: .gitattributes gets `package.json merge=version-manager`; git config gets merge.version-manager.name and merge.version-manager.driver. The install command should write both.\n\nCAVEAT STILL UNVERIFIED: a merge driver is LOCAL git config, so it does not run for merges performed on GitHub's servers (the merge/squash button). Server-side merges would still conflict and block the button. Verify this before promising the driver fixes the PR workflow, and if it is confirmed, say so in the README rather than leaving the impression it is solved.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-16T14:46:52.164225Z","created_by":"jhaa","updated_at":"2026-09-19T11:10:34.408186Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"version-manager-70i.8","depends_on_id":"version-manager-70i","type":"parent-child","created_at":"2026-09-16T14:46:52.164225Z","created_by":"jhaa","metadata":"{}","thread_id":""}]}
{"id":"version-manager-70i.9","title":"Delete docs/plans/2026-03-27_committed-version-modes.md (violates beads rule)","status":"closed","priority":3,"issue_type":"chore","created_at":"2026-09-16T14:46:52.261751Z","created_by":"jhaa","updated_at":"2026-09-19T11:05:51.159674Z","closed_at":"2026-09-19T11:05:51.159491Z","close_reason":"Deleted docs/plans/2026-03-27_committed-version-modes.md in 35987e0. The file was added by this branch only (never on main), so the deletion is fully recoverable from git history, and its content is preserved verbatim in the epic's design field.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"version-manager-70i.9","depends_on_id":"version-manager-70i","type":"parent-child","created_at":"2026-09-16T14:46:52.261751Z","created_by":"jhaa","metadata":"{}","thread_id":""}]}
